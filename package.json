{
  "name": "@dorokuma/herdsman",
  "version": "0.6.0",
  "description": "Agent history daemon and runtime bridges for Herdr-managed coding agents. Forked from @ryonakae/shepherd.",
  "type": "module",
  "files": [
    "dist",
    "drizzle"
  ],
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "herdsman": "dist/src/cli/herdsman.js"
  },
  "keywords": [
    "herdr",
    "pi-coding-agent",
    "herdr-plugin",
    "pi-extension"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dorokuma/herdsman.git"
  },
  "bugs": {
    "url": "https://github.com/dorokuma/herdsman/issues"
  },
  "homepage": "https://github.com/dorokuma/herdsman#readme",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=11.9.0"
  },
  "scripts": {
    "build": "pnpm clean:dist && tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json",
    "check": "pnpm typecheck && pnpm test && pnpm lint && pnpm format:check && pnpm db:check && pnpm package:check && pnpm pi-package:check && pnpm herdr-plugin:check",
    "clean:dist": "node scripts/clean-dist.mjs",
    "db:check": "drizzle-kit check --config drizzle.config.ts",
    "db:generate": "drizzle-kit generate --config drizzle.config.ts",
    "db:migrate": "tsx src/db/migrate.ts",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "herdr-plugin:check": "pnpm --dir packages/herdsman-herdr-plugin typecheck && pnpm --dir packages/herdsman-herdr-plugin package:check",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "package:check": "node scripts/check-root-package.mjs",
    "pi-package:check": "pnpm --dir packages/herdsman-pi typecheck && (cd packages/herdsman-pi && npm pack --dry-run --json > /dev/null)",
    "pnpm:devPreinstall": "husky",
    "prepack": "pnpm build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.49",
    "ajv": "^8.20.0",
    "drizzle-orm": "^0.45.2",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.1",
    "@earendil-works/pi-coding-agent": "^0.80.6",
    "@earendil-works/pi-tui": "^0.80.6",
    "@types/node": "^24.13.2",
    "drizzle-kit": "^0.31.10",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "tsc-alias": "^1.8.17",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,jsonc}": "biome check --write --no-errors-on-unmatched"
  },
  "author": "dorokuma",
  "contributors": [
    "Ryo Nakae (https://github.com/ryonakae)"
  ]
}
